export class Serie {

    constructor(public id: number, public name: string, public channel: string, public seasons: number, public resumen: string, public website: string, public image: string) { }
}